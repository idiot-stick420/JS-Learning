"use strict"; // treat all JS code as newer version, no thing like no strict, therefore always newer version
alert(3 + 3) // Can't run because this is online not docs

let name = "Ujwal"
let age = 18
let isAdult = true

// null is a standAlone value, a different data type
// str = "" => String not null
// str = null => is null
// undefined
// symbol => A unique smthn

// object
// null is an object, whereas undefined is it's own datatype

